services:
  api-1:
    image: joaoxyz/rinha-api:v1
    networks:
      - payment-processor
      - app-network
    environment:
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
      - health-check
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "75MB"

  api-2:
    image: joaoxyz/rinha-api:v1
    networks:
      - payment-processor
      - app-network
    environment:
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
      - health-check
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "75MB"

  health-check:
    image: joaoxyz/rinha-health-check:v1
    networks:
      - payment-processor
      - app-network
    environment:
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: "25MB"

  worker:
    image: joaoxyz/rinha-worker:v1
    networks:
      - payment-processor
      - app-network
    environment:
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
      - health-check
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: "75MB"

  nginx:
    image: nginx:latest
    ports:
      - "9999:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api-1
      - api-2
    networks:
      - app-network
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "50MB"

  redis:
    image: redis:alpine
    command: redis-server --save ""
    networks:
      - app-network
    deploy:
        resources:
          limits:
            cpus: "0.1"
            memory: "50MB"

networks:
  app-network:
    driver: bridge
  payment-processor:
    external: true
