name: Simple Bash Workflow

on:
    push:
        branches:
            - check-test
    pull_request:

jobs:
    bash-example:
        runs-on: ubuntu-latest
        steps:
            - name: Teste 01
              run: |
                echo "01"
                ls -lh
                exit 0
            - name: Teste 02
              run: |
                echo "Hello from GitHub Actions!"
                ls -l
                exit 1